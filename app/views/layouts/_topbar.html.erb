<div class="top-bar">

  <div class="left">
    <%= link_to "Rescue", questions_path, id: "logo" %><%= link_to "Mission", questions_path, id: "logo-light" %>
  </div>

  <ul class="sign-in">
    <% if current_user %>
      <li>Signed in as <%= current_user.username %></a></li>
      <li> <%= link_to "Sign Out", signout_path, id: "sign_out" %></li>
    <% else %>
      <li> <%= link_to "Sign In with Facebook", "/auth/facebook", id: "sign_in" %></li>
    <% end %>
  </ul>
</div>
